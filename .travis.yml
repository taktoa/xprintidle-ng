sudo: false

language: c

compiler:
  - gcc
  - clang

addons:
  apt:
    packages:
    - libxss-dev
    - build-essential
    - automake
    - autoconf
    - libtool
    - gettext
    - pkg-config

script:
  - ./bootstrap
  - ./configure
  - make
  - make test
